// Entry point for the Electronic Sign Simulator.
//
// This minimal launcher calls the CLI interface.
// Keeping the entry point simple allows the CLI module to be
// tested independently if refactored into a package structure.
package main

func main() {
	Run()
}
